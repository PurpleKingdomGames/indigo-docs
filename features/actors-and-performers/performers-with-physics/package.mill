package build.features.`actors-and-performers`.`performers-with-physics`

import indigoplugin.*
import build.GameModule

object `package` extends GameModule:

  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("Performers with Physics Example")
      .withWindowSize(800, 600)
