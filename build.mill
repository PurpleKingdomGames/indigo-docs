//| mvnDeps:
//|   - "io.indigoengine:mill-indigo_mill1_3:0.30.0-M1-PREVIEW"
//|   - "org.typelevel::scalac-options:0.1.11"

package build

import mill.*
import mill.scalalib.*
import mill.scalajslib.*
import mill.scalajslib.api.*
import mill.api.BuildCtx
import mill.scalalib.scalafmt.*

import indigoplugin.*
import org.typelevel.scalacoptions.*

// object demos extends mill.Module {

  // object `the-cursed-pirate` extends GameModule {

  //   override def indigoAssets =
  //     Task.Source(workspaceDir / os.RelPath("demos/the-cursed-pirate/assets"))

  //   def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
  //     makeIndigoOptions("The Cursed Pirate - Made with Indigo")
  //       .withWindowWidth(1280)
  //       .withWindowHeight(720)
  //       .withBackgroundColor("black")
  //       .excludeAssetPaths {
  //         case p if p.contains("unused")                       => true
  //         case p if p.contains("Captain Clown Nose Data.json") => true
  //       }
  //       .excludeAssets {
  //         case p if p.toString.endsWith(".DS_Store") => true
  //         case _                                     => false
  //       }

  //   override def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
  //     IndigoGenerators("pirate.generated")
  //       .listAssets("Assets")
  //       .generateConfig("Config")
  //       .embedAseprite(
  //         "CaptainAnim",
  //         workspaceDir / "demos" / "the-cursed-pirate" / "assets" / "captain" / "Captain Clown Nose Data.json"
  //       )

  //   override def mvnDeps =
  //     super.mvnDeps() ++ Seq(mvn"org.scalacheck::scalacheck:1.15.3")
  // }

// }

// object features extends mill.Module {

//   // This is the root directory of the workspace / project.
//   lazy val workspaceDir: os.Path =
//     BuildCtx.workspaceRoot

//   object `actors-and-performers` extends mill.Module {

//     object actors extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Actors Example")
//           .withWindowSize(800, 600)
//     }

//     object performers extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Performers Example")
//           .withWindowSize(800, 600)
//     }

//     object `performers-with-physics` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Performers with Physics Example")
//           .withWindowSize(800, 600)
//     }

//   }

//   object context extends GameModule {
//     def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//       makeIndigoOptions("Context Example")
//   }

//   object materials extends mill.Module {

//     object bitmap extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Bitmap Material Example")
//     }

//     object filltypes extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Fill Types Example")
//     }

//     object imageeffects extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("ImageEffects Material Example")
//     }

//   }

//   object physics extends mill.Module {

//     object basics extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Basic Physics Example")
//           .withWindowSize(800, 600)
//     }

//   }

//   object primitives extends mill.Module {

//     object blank extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Blank Entity Example")
//     }

//     object clip extends GameModule {

//       override def indigoAssets =
//         Task.Source(workspaceDir / os.RelPath("features/primitives/clip/assets"))

//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Clip Example")
//           .withWindowWidth(550)
//           .withWindowHeight(400)
//           .withBackgroundColor("black")

//       override def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
//         IndigoGenerators("indigoexamples.generated")
//           .listAssets("Assets")
//           .generateConfig("Config")

//     }

//     object graphic extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Graphic Example")
//     }

//     object group extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Group Example")
//     }

//     object shapes extends mill.Module {

//       object box extends GameModule {
//         def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//           makeIndigoOptions("Box Shape Example")
//       }

//       object circle extends GameModule {
//         def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//           makeIndigoOptions("Circle Shape Example")
//       }

//       object line extends GameModule {
//         def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//           makeIndigoOptions("Line Shape Example")
//       }

//       object polygon extends GameModule {
//         def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//           makeIndigoOptions("Polygon Shape Example")
//       }

//     }

//     object sprite extends GameModule {

//       override def indigoAssets =
//         Task.Source(workspaceDir / os.RelPath("features/primitives/sprite/assets"))

//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Sprite Example")
//           .withWindowWidth(550)
//           .withWindowHeight(400)
//           .withBackgroundColor("black")

//       override def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
//         IndigoGenerators("indigoexamples.generated")
//           .listAssets("Assets")
//           .generateConfig("Config")

//     }

//     object text extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Text Example")

//       /** The generator includes the usual config and asset listing generation, but also includes a
//         * font embedding step. It reads a font file from somewhere in your file system and outputs
//         * the rendered font to you asset directory. If you need more variations of size or style
//         * then you need to add another embedding step.
//         */
//       override def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
//         IndigoGenerators("generated")
//           .generateConfig("Config")
//           .embedFont(
//             moduleName = "KiwiSodaFont",
//             font = workspaceDir / "generator-data" / "KiwiSoda.ttf",
//             fontOptions = FontOptions(
//               fontKey = "KiwiSoda",
//               fontSize = 16,
//               charSet = CharSet.ASCII,
//               color = RGB.White,
//               antiAlias = false,
//               layout = FontLayout.normal
//             ),
//             imageOut = workspaceDir / "assets" / "generated"
//           )
//           .listAssets("Assets")
//     }

//   }

//   object scenes extends mill.Module {

//     object minimal extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Minimal Scene Example")
//     }

//   }

//   object shaders extends mill.Module {

//     object basic extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Basic Custom Shader")
//     }

//     object blending extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Blending")
//     }

//   }

//   object time extends mill.Module {

//     object signals extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Signals Example")
//     }

//     object `time-varying-values` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Time Varying Values Example")
//     }

//   }

//   object ui extends mill.Module {

//     object button extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Button")
//     }

//     object `component-group` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - ComponentGroup")
//     }

//     object `component-list` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - ComponentList")
//     }

//     object custom extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Custom Components")
//     }

//     object label extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Label")
//     }

//     object hitarea extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - HitArea")
//     }

//     object input extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Input")
//     }

//     object maskedpane extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - MaskedPane")
//     }

//     object scrollpane extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - ScrollPane")
//     }

//     object switch extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Switch")
//     }

//     object textarea extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - TextArea")
//     }

//     object window extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Window")
//     }

//   }

// }

// object guides extends mill.Module {

//   // This is the root directory of the workspace / project.
//   lazy val workspaceDir: os.Path =
//     BuildCtx.workspaceRoot

//   object actors extends mill.Module {

//     object `actors-with-physics` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Actors with Physics Example")
//           .withWindowSize(800, 600)
//     }

//   }

//   object animation extends mill.Module {

//     object `part-1-basics` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Part 1: Animation Fundamentals")
//           .withWindowSize(800, 600)
//     }

//     object `part-2-signals` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Part 2: Signals")
//           .withWindowSize(800, 600)
//     }

//     object `part-3-timelines` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Part 3: Timeline Animations")
//           .withWindowSize(800, 600)
//     }

//     object `part-4-shaders` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Part 4: Shader Animations")
//           .withWindowSize(800, 600)
//     }

//   }

//   object assets extends mill.Module {

//     object preloader extends GameModule {

//       override def indigoAssets =
//         Task.Source(workspaceDir / os.RelPath("guides/assets/preloader/assets"))

//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Preloader / Loading Screen Example")
//           .withWindowWidth(1280)
//           .withWindowHeight(720)
//           .withBackgroundColor("black")
//           .excludeAssetPaths {
//             case p if p.contains("unused")                       => true
//             case p if p.contains("Captain Clown Nose Data.json") => true
//           }

//       override def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
//         IndigoGenerators("preloader.generated")
//           .listAssets("Assets")
//           .generateConfig("Config")
//           .embedAseprite(
//             "CaptainAnim",
//             workspaceDir / "guides" / "assets" / "preloader" / "assets" / "captain" / "Captain Clown Nose Data.json"
//           )

//     }
//   }

//   object importers extends mill.Module {

//     object `aseprite-embedded` extends GameModule {

//       override def indigoAssets =
//         Task.Source(workspaceDir / os.RelPath("guides/importers/aseprite-embedded/assets"))
        
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Aseprite (Embedded)")
//           .withWindowWidth(550)
//           .withWindowHeight(400)
//           .withBackgroundColor("black")
//           .excludeAssetPaths {
//             case p if p.contains("Captain Clown Nose Data.json") => true
//           }

//       override def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
//         IndigoGenerators("indigoexamples.generated")
//           .listAssets("Assets")
//           .generateConfig("Config")
//           .embedAseprite(
//             "CaptainAnim",
//             workspaceDir / "guides" / "importers" / "aseprite-embedded" / "assets" / "Captain Clown Nose Data.json"
//           )
//     }

//     object `tiled-loaded` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Tiled (Loaded)")
//     }

//   }

//   object scenes extends mill.Module {

//     object `scene-management` extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("Scene Management")
//     }

//   }

//   object ui extends mill.Module {

//     object radiobuttons extends GameModule {
//       def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
//         makeIndigoOptions("UI Components - Radio buttons")
//     }

//   }

// }


trait GameModule extends MillIndigoJS with ScalafmtModule {
  def scalaVersion   = "3.8.3"
  def scalaJSVersion = "1.21.0"

  override def scalacOptions = Task {
    val baseOptions = ScalacOptions.tokensForVersion(
      ScalaVersion.unsafeFromString(scalaVersion()),
      ScalacOptions.default ++
        ScalacOptions.fatalWarningOptions
    )
    
    // "-Xfatal-warnings" removed because it's been replaced with "-Werror"
    val excluded = Set(
      "-Xfatal-warnings",
      "-Wunused:all",
      "-Wunused:locals",
      "-Wunused:params",
      "-Wunused:explicits",
      "-Wnonunit-statement"
    )
    val filtered = baseOptions.filterNot(excluded.contains)

    super.scalacOptions() ++ filtered ++ Seq("-Werror")
  }

  def indigoOptions(assetsDirectory: os.Path): IndigoOptions

  def indigoAssets =
    Task.Source(BuildCtx.workspaceRoot / "assets")

  def makeIndigoOptions(title: String): IndigoOptions =
    IndigoOptions.defaults
      .withTitle(title)
      .withWindowSize(550, 400)
      .withBackgroundColor("black")
      .excludeAssets {
        case p if p.endsWith(os.RelPath.rel / ".gitkeep")  => true
        case p if p.endsWith(os.RelPath.rel / ".DS_Store") => true
        case _                                             => false
      }

  def indigoGenerators(assetsDirectory: os.Path): IndigoGenerators =
    IndigoGenerators("generated")
      .generateConfig("Config")
      .embedFont(
        moduleName = "DefaultFont",
        font = BuildCtx.workspaceRoot / "generator-data" / "Minecraft.ttf",
        fontOptions = FontOptions(
          fontKey = "DefaultFont",
          fontSize = 16,
          charSet = CharSet.ASCII,
          color = RGB.White,
          antiAlias = false,
          layout = FontLayout.default
        ),
        imageOut = BuildCtx.workspaceRoot / "assets" / "generated"
      )
      .listAssets("Assets")

  val indigoVersion = "0.30.0-M1-PREVIEW"

  def mvnDeps =
    Seq(
      mvn"io.indigoengine::indigo-json-circe::$indigoVersion",
      mvn"io.indigoengine::indigo::$indigoVersion",
      mvn"io.indigoengine::indigo-extras::$indigoVersion"
    )

  object test extends ScalaJSTests {
    def mvnDeps = Seq(
      mvn"org.scalameta::munit::1.2.2"
    )

    override def moduleKind = Task(mill.scalajslib.api.ModuleKind.CommonJSModule)

    def testFramework = Task("munit.Framework")
  }

}
